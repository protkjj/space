# 우주로버 챌린지 — Traversability 매핑 로버 (ROS 2)

> 이 문서는 프로젝트 전체 맥락이다. 코드를 짜기 전에 반드시 읽고,
> 여기 적힌 임무 논리·확정 사항·용어를 기준으로 작업한다.

---

## 0. 한 줄 요약

4륜 스키드 스티어 소형 로버(설계 추정 **2,725 g**)가 비정형 지형을 주행하며
**Depth·IMU·엔코더를 융합해 "주행가능성 지도(Traversability Map)"를 생성**하고,
안전이 검증된 지점에 특징점 마커를 투하한다.
핵심 지표는 **실측 슬립률**이다.

> **질량 표기 주의.** 3 kg은 대회 **상한 제약**이지 설계값이 아니다
> (`docs/mission_requirements.md`: "3 kg maximum"). 설계 추정치는 2,725 g이고,
> 시뮬레이션은 이 값을 쓴다 — 정본은
> `space_description/config/rover_geometry.yaml`의 `mass_total`이며 URDF가 정확히
> 그 합이 되도록 파생된다. 이전에는 세 숫자가 모두 달랐고(상한 3.0 / 설계 2.725 /
> sim 2.450) sim이 10.1% 가벼웠다. 슬립이 핵심 측정량인 임무에서 가벼운 모델은
> 슬립이 덜 나고 견인이 더 좋게 나오므로, 그 위에서 한 검증은 검증이 아니다.
> 실기 조립 후 실측이 나오면 `mass_total`을 그 값으로 교체한다.

---

## 1. 임무 논리 (가장 중요 — 여기서 벗어나지 말 것)

### 1.1 왜 슬립률인가
- Depth 카메라는 **기하 정보(경사·고도)만** 준다. 토양이 실제로 어떻게
  반응하는지(미끄러지는지)는 알 수 없다.
- 같은 15° 경사라도: 슬립 10% → 안전 / 슬립 40% → 침하 위험. **정반대.**
- 이 차이는 **실제로 그 위를 굴러본 로버만** 안다. 위성도 시뮬도 못 준다.
  → 소형 로버의 대체 불가능한 가치.

### 1.2 슬립률 계산
```
λ = (V_wheel − V_actual) / V_wheel
```
- `V_wheel` : RoboClaw 엔코더 → **슬립에 오염됨**(헛돌아도 "갔다"고 보고)
- `V_actual` : OAK-D VIO → **슬립과 무관한 실제 이동**

### 1.3 순환논리가 아님 (반드시 지킬 설계 전제)
- VIO는 **바퀴를 전혀 참조하지 않는다.** 카메라가 본 지형의 시각적 흐름 +
  IMU로 이동을 추정한다. 따라서 엔코더와 **독립 측정원**이다.
- 두 값의 차이는 순환 없이 계산된다.
- 단, 슬립률은 **절대 정밀 측정이 아니라 "상대 판정"**으로 쓴다.
  "이 구역이 저 구역보다 슬립이 심하다"는 판정은 VIO 오차가 있어도 유효.

### 1.4 판단 임계값
| 판정 | 조건 | 행동 |
|---|---|---|
| **안전** | 경사 < 20° AND 슬립률 < 15% AND 거칠기 < σ₀ | 마커 투하 + 좌표 기록 |
| 중간 | 일부 미충족 | 감속 후 추가 데이터 수집 |
| **위험** | 경사 ≥ 20° OR 슬립률 ≥ 40% OR 장애물·단차 | 회피/감속, 지도에만 기록 |

### 1.5 마커 투하 로직 (★자주 헷갈림 — 반드시 이 방향)
- 마커는 **"위험한 곳"이 아니라 "안전이 검증된(Green Zone)" 지점에 투하한다.**
- 이유: 후속 대형 로버는 우리 지도로 Traversability 높은 경로를 택한다.
  → 마커 위치와 대형 로버의 최적 경로가 **같은 기준**으로 정해져 자동 일치.
  → 마커를 발견한 대형 로버는 ① 경로 검증 증거 확보 ② 특징점 없는
     레골리스에서 SLAM 드리프트 리셋 앵커로 활용.
- 마커 투하는 **확장 기능**이다. 디스펜서가 고장 나도 핵심 임무(지도 생성)는
  독립적으로 계속되어야 한다. 이 분리 구조를 코드에서도 유지할 것.

---

## 2. 확정 하드웨어 (변경 금지 — 이 스펙 기준으로 작성)

| 역할 | 부품 | 핵심 |
|---|---|---|
| 연산 | Jetson Orin Nano Super 8GB | 상위 자율·SLAM·지도·미션 관리 |
| 자세·안전 | Pixhawk 6X + **ArduPilot Rover** | 차체 자세, failsafe, 서보, 모드 |
| 모터 드라이버 | RoboClaw 2x15A (V6B) | 좌/우 2채널, 엔코더 폐루프, 전류제한 |
| 구동 | Pololu 37D 100:1 브러시DC ×4 | 스키드 스티어 |
| 카메라 | OAK-D Pro W | Active Stereo(IR) + Depth + BNO086 IMU |
| 전원 | 4S LiPo → PM02D(전류센싱) → 분배 | — |
| 마커 | 서보(MG90) 구동 디스펜서 | 단발 투하 |

### 2.1 이중 IMU — 중복이 아니라 역할 분리 (혼동 금지)
- **Pixhawk IMU** (차체 중심) → 차체 자세(Roll/Pitch/Yaw), 전복 경고, failsafe.
  ArduPilot 검증 EKF 그대로 사용.
- **BNO086** (OAK-D 내장, 카메라와 HW 시간동기) → **VIO 전용.**
- 둘을 섞지 말 것. VIO는 반드시 BNO086 + 스테레오 기반.

---

## 3. 소프트웨어 아키텍처 (ROS 2 Humble)

### 3.1 책임 분리
- **Jetson (ROS 2)** : 카메라 처리, VIO/SLAM, 지형 재구성, 위험 검출,
  마커 지점 선정, 경로계획, 미션 상태관리, 로깅.
- **Pixhawk (ArduPilot Rover)** : 차체 자세, 로버 모드, failsafe, 명령 검증,
  모터 명령 출력, 서보 구동, 배터리·헬스 모니터, 수동 오버라이드.
- **RoboClaw** : 좌/우 채널 제어, 쿼드러처 엔코더, 휠속도 폐루프,
  전류 제한(스톨 시 기어박스 보호), 가감속 제한.

### 3.2 노드 구성
| 노드 | 역할 | 입력 → 출력 |
|---|---|---|
| `oak_d_node` (DepthAI) | 센서 드라이버 | → `/points`(PointCloud2), `/image_raw`, `/imu`(BNO086), `/vio/odom` |
| `mavros` 또는 AP_DDS | Pixhawk 브리지 | → `/mavros/imu/data`(차체 자세) |
| `roboclaw_node` | 드라이버 | `/cmd_vel` 수신 → `/wheel_odom`(V_wheel) |
| **`robot_localization` (EKF)** | 융합 | VIO + 엔코더 + IMU → `/odometry/filtered` |
| `elevation_mapping` | 고도맵 | `/points` + Pose → 고도 그리드 → 법선 → 경사도 |
| **`slip_estimator`** (자체) | 슬립 산출 | `/wheel_odom` + `/vio/odom` → `/slip`(λ) |
| **`traversability_node`** (자체) | 비용맵 | 경사 θ + 거칠기 σ + 슬립 λ → `S = f(θ,σ,λ)` 격자점수 |
| `nav2` | 경로계획 | Traversability 비용맵 → Global(A*) / Local(DWA) |
| **`marker_dispenser`** (자체) | 투하 | S ≥ 임계값 → 서보 PWM + 좌표 기록 |
| `mission_manager` (자체) | 상태머신 | 아래 3.3 |

### 3.3 미션 상태머신
```
INITIALIZE → SELF_CHECK → LOCALIZE → EXPLORE → DETECT_HAZARD
→ SELECT_MARKER_LOCATION → APPROACH → (정지·안정확인) → DEPLOY_MARKER
→ VERIFY_DEPLOYMENT → LOG_LOCATION → CONTINUE or RETURN
```
Failsafe 전이(반드시 구현):
- VIO/위치추정 상실 → 정지, 재초기화
- 카메라 고장 → 정지, 원격조종 전환 (필수 임무 유지)
- 통신 두절 → 안전 정지 (ArduPilot failsafe)
- 배터리 저전압 → 임무 중단
- 모터 스톨 → RoboClaw 전류제한 + 정지 후 복구 시도
- 서보 걸림 → 기록 후 **핵심 임무 계속** (확장/핵심 분리)

### 3.4 마커 투하 시퀀스 (정지 투하)
접근 → 정지 → 차체 안정 확인(Pixhawk 자세) → 서보 작동 →
투하 확인(우선 서보 위치, 이후 카메라 시각 확인) → 좌표+위험정보 기록 → 계속.
주행 중 투하 아님. 정지 투하로 마커 위치 정확도 확보.

### 3.5 경량화(실시간성) 규칙
전체 포인트클라우드 처리 금지. **주행 궤적 전방 1 m ROI만** 연산
(~300k → ~30k 포인트). 로버 속도 0.1~0.3 m/s 기준 3~10초 대응시간 확보.

---

## 4. 개발 원칙

- **지형 위험 검출의 1차 구현은 geometry 기반**(경사·단차·거칠기·양/음 장애물).
  AI 세그멘테이션은 나중 옵션. 거대한 커스텀 달 데이터셋에 의존하지 말 것.
- 위치추정은 **"multi-sensor localization"**으로 표현. "완벽한 달 항법을
  즉시 재현한다"고 주장하지 말 것.
- ArduPilot 연동은 **ROS 2 Humble** 기준. AP_DDS(네이티브) 또는 MAVROS2 둘 다 가능.
- 좌표계·타임스탬프는 노드 간 반드시 동기화·명시.

---

## 5. 지금 부탁하는 작업

(← 여기에 그때그때 시킬 구체 작업을 적는다. 예:)
- [ ] ROS 2 워크스페이스 + 패키지 스캐폴딩 생성
- [ ] `slip_estimator` 노드 스켈레톤 (구독/발행 인터페이스만)
- [ ] `robot_localization` EKF 설정 YAML 초안
- [ ] ...

먼저 인터페이스(토픽·타입)부터 정의하고, 구현은 그다음 단계로 나눠서 진행할 것.