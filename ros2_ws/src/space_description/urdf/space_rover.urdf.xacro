<?xml version="1.0"?>
<robot name="space_rover" xmlns:xacro="http://ros.org/wiki/xacro">
  <xacro:property name="base_length" value="0.32"/>
  <xacro:property name="base_width" value="0.26"/>
  <xacro:property name="base_height" value="0.10"/>
  <xacro:property name="wheel_radius" value="0.112"/>
  <xacro:property name="wheel_width" value="0.045"/>
  <xacro:property name="wheel_x" value="0.145"/>
  <xacro:property name="wheel_y" value="0.175"/>
  <xacro:property name="wheel_z" value="0.02"/>

  <material name="chassis_dark"><color rgba="0.10 0.12 0.14 1.0"/></material>
  <material name="wheel_gray"><color rgba="0.35 0.38 0.40 1.0"/></material>
  <material name="camera_blue"><color rgba="0.10 0.25 0.80 1.0"/></material>

  <link name="base_footprint"/>

  <joint name="base_joint" type="fixed">
    <parent link="base_footprint"/>
    <child link="base_link"/>
    <origin xyz="0 0 ${wheel_radius + base_height * 0.5 - 0.025}" rpy="0 0 0"/>
  </joint>

  <link name="base_link">
    <visual>
      <geometry><box size="${base_length} ${base_width} ${base_height}"/></geometry>
      <material name="chassis_dark"/>
    </visual>
    <collision>
      <geometry><box size="${base_length} ${base_width} ${base_height}"/></geometry>
    </collision>
    <inertial>
      <mass value="1.8"/>
      <origin xyz="0 0 0" rpy="0 0 0"/>
      <inertia ixx="0.018" ixy="0" ixz="0" iyy="0.022" iyz="0" izz="0.030"/>
    </inertial>
  </link>

  <xacro:macro name="wheel" params="prefix x y">
    <link name="${prefix}_wheel">
      <visual>
        <origin xyz="0 0 0" rpy="1.57079632679 0 0"/>
        <geometry><cylinder radius="${wheel_radius}" length="${wheel_width}"/></geometry>
        <material name="wheel_gray"/>
      </visual>
      <collision>
        <origin xyz="0 0 0" rpy="1.57079632679 0 0"/>
        <geometry><cylinder radius="${wheel_radius}" length="${wheel_width}"/></geometry>
      </collision>
      <inertial>
        <mass value="0.14"/>
        <origin xyz="0 0 0" rpy="0 0 0"/>
        <inertia ixx="0.00045" ixy="0" ixz="0" iyy="0.00045" iyz="0" izz="0.00045"/>
      </inertial>
    </link>
    <joint name="${prefix}_wheel_joint" type="continuous">
      <parent link="base_link"/>
      <child link="${prefix}_wheel"/>
      <origin xyz="${x} ${y} ${wheel_z - base_height * 0.5}" rpy="0 0 0"/>
      <axis xyz="0 1 0"/>
    </joint>
  </xacro:macro>

  <xacro:wheel prefix="left_front" x="${wheel_x}" y="${wheel_y}"/>
  <xacro:wheel prefix="left_rear" x="-${wheel_x}" y="${wheel_y}"/>
  <xacro:wheel prefix="right_front" x="${wheel_x}" y="-${wheel_y}"/>
  <xacro:wheel prefix="right_rear" x="-${wheel_x}" y="-${wheel_y}"/>

  <link name="camera_link">
    <visual>
      <geometry><box size="0.055 0.025 0.025"/></geometry>
      <material name="camera_blue"/>
    </visual>
    <collision>
      <geometry><box size="0.055 0.025 0.025"/></geometry>
    </collision>
    <inertial>
      <mass value="0.08"/>
      <inertia ixx="0.00002" ixy="0" ixz="0" iyy="0.00002" iyz="0" izz="0.00002"/>
    </inertial>
  </link>

  <joint name="camera_joint" type="fixed">
    <parent link="base_link"/>
    <child link="camera_link"/>
    <origin xyz="0.17 0 0.07" rpy="0 0 0"/>
  </joint>

  <!-- REP-103 optical coordinates: Z forward, X right, Y down. -->
  <link name="camera_optical_frame"/>
  <joint name="camera_optical_joint" type="fixed">
    <parent link="camera_link"/>
    <child link="camera_optical_frame"/>
    <origin xyz="0 0 0" rpy="-1.57079632679 0 -1.57079632679"/>
  </joint>

  <link name="imu_link">
    <inertial>
      <mass value="0.01"/>
      <inertia ixx="0.000001" ixy="0" ixz="0" iyy="0.000001" iyz="0" izz="0.000001"/>
    </inertial>
  </link>

  <joint name="imu_joint" type="fixed">
    <parent link="base_link"/>
    <child link="imu_link"/>
    <origin xyz="0 0 0.03" rpy="0 0 0"/>
  </joint>
</robot>
